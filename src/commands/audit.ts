/**
 * `custos audit` — shows recent audit events from MongoDB.
 *
 * Not implemented yet. Future behavior (AGENTS.md "custos audit"): connect
 * to MongoDB, fetch recent events sorted by createdAt descending, render
 * eventType, finding title, user, and timestamp.
 */
export async function runAudit(): Promise<void> {
  console.log("custos audit: not implemented yet.");
}
