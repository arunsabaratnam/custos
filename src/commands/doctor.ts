/**
 * `custos doctor` — validates environment before a demo.
 *
 * Not implemented yet. Future behavior (AGENTS.md "custos doctor"): confirm
 * Node version, Git availability, check BACKBOARD_API_KEY/BASE_URL,
 * AUTH0_DOMAIN/CLIENT_ID/AUDIENCE, MONGODB_URI + connection test, and print
 * a pass/fail summary per check.
 */
export async function runDoctor(): Promise<void> {
  console.log("custos doctor: not implemented yet.");
}
